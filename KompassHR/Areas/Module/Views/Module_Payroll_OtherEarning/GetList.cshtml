@model KompassHR.Areas.Module.Models.Module_Payroll.OtherEarningList
@{
    ViewBag.Title = "GetList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script src="~/scripts/jquery-1.10.2.js"></script>
<script src="~/scripts/jquery-1.10.2.min.js"></script>
<script src="~/scripts/jquery.validate.js"></script>
<script src="~/scripts/jquery.validate.min.js"></script>
<script src="~/assets/plugins/sweetalert/dist/sweetalert.min.js"></script>


<style>
    .Tooltip {
        position: relative;
    }

        .Tooltip button {
            width: 35px;
        }

        .Tooltip:hover .Tooltip-text {
            display: flex;
            position: absolute;
        }

        .Tooltip .Tooltip-text {
            z-index: 1;
            min-width: 45px;
            max-width: fit-content;
            background-color: black;
            color: #fff;
            top: 40px;
            right: -18px;
            padding: 5px 8px;
            border-radius: 6px;
            display: none;
            justify-content: center;
            position: absolute;
            margin-right: 10px;
        }

            .Tooltip .Tooltip-text::after {
                content: "";
                position: absolute;
                top: -20px;
                right: 20px;
                border-width: 10px;
                border-style: solid;
                border-color: transparent transparent black transparent;
            }

    .ListBtn {
        font-size: 10px !important;
        padding: 5px 8px !important;
        border: 1px solid lightgray !important;
    }

    /*.trheading {
        background-color: rgba(233, 236, 239, 0.5) !important;
    }*/

    tr td {
        padding: 4px !important;
        font-weight: 600;
    }

    .table-striped > tbody > tr:nth-of-type(odd) > * {
        --bs-table-accent-bg: rgba(233, 236, 239, 0.3) !important;
    }

    .table-wrap::-webkit-scrollbar-track {
        -webkit-box-shadow: inset 0 0 6px rgba(0, 0, 0, 0.3);
        background-color: #F5F5F5;
        border-radius: 10px;
    }

    .table-wrap::-webkit-scrollbar {
        width: 6px;
        height: 10px;
        background-color: #F5F5F5;
    }

    .table-wrap::-webkit-scrollbar-thumb {
        background-color: #777;
        border-radius: 10px;
    }

    label.error, .Error {
        text-align: left;
        height: 25px;
        color: red;
        font-size: 12px;
    }

    .error {
        text-align: left;
        height: 35px;
    }

    /*table first column fixed*/
    .table-wrap {
        width: 100%;
        overflow: auto;
    }

    .table-scroll table {
        width: 100%;
        margin: auto;
        border-collapse: separate;
        border-spacing: 0;
    }

    .table-scroll th, .table-scroll td {
        padding: 5px 10px;
        border: 1px solid lightgray;
        background: #fff;
        white-space: nowrap;
        vertical-align: top;
    }

    .table-scroll th {
        background-color: rgba(233, 236, 239, 1) !important;
    }

    .div1 td:nth-child(1) {
        position: sticky;
        left: 0;
    }

    .div1 td:nth-child(2) {
        position: sticky;
        left: 62px;
    }

    .fixed-side {
        position: sticky;
        left: 0;
    }

    .fixed-side1 {
        position: sticky;
        left: 62px;
    }
</style>

<div id="content" class="app-content">
    <div class="row" style="margin-bottom: 10px;">
        <div class="col-md-12" style="display: flex; justify-content: space-between; align-items: center;">
            <h1 style="font-size: 18px; font-family: roboto; transform: translateY(4px); margin: 0;">
                Variable Earning
            </h1>
            <ol style="list-style: none; padding: 0; margin: 0; display: flex;">
                <li style="margin-right: 10px;">
                    <a href="@Url.Action("Dashboard", "Dashboard", new { area = "" })">
                        <img src="~/assets/MenuImages/Home.png" style="max-width: 100%; height: auto;" alt="Home">
                    </a>
                </li>
                <li>
                    <a href="@Url.Action("Module_Payroll_Menu", "Module_Payroll_Menu", new {id = 6, ScreenId=159, area = "Module" })">
                        <img src="~/assets/MenuImages/Back.png" style="max-width: 100%; height: auto;" alt="Back">
                    </a>
                </li>
            </ol>
        </div>
    </div>
    
    <div class="row">
        <div class="col-md-12">
            <!-- BEGIN panel -->
            <div class="panel panel-inverse" data-sortable-id="index-1">
                <div class="panel-heading" style="background-color:@Session["HeaderColor"]; color:@Session["HeaderTextColor"]">
                    <h4 class="panel-title">Variable Earning   - List</h4>
                    <div class="panel-heading-btn">
                        <a href="javascript:;" class="btn btn-xs btn-icon btn-default" data-toggle="panel-expand"><i class="fa fa-expand"></i></a>
                        <a href="javascript:;" class="btn btn-xs btn-icon btn-success" data-toggle="panel-reload"><i class="fa fa-redo"></i></a>
                        <a href="javascript:;" class="btn btn-xs btn-icon btn-warning" data-toggle="panel-collapse"><i class="fa fa-minus"></i></a>
                        <a href="javascript:;" class="btn btn-xs btn-icon btn-danger" data-toggle="panel-remove"><i class="fa fa-times"></i></a>
                    </div>
                </div>
                <div class="alert alert-yellow rounded-0 pb-0 d-flex align-items-center mb-0 text-black">
                    <p class="pb-0">
                        This is the list of variable earning created.
                    </p>
                </div>
                <div class="panel-body">
                    <div class="row">
                        <div class="col-md-2">
                            <div>
                                <a href="@Url.Action("Module_Payroll_OtherEarning", "Module_Payroll_OtherEarning")">
                                <button type="button" id="btnAdd" class="btn btn-primary btn-sm  mb-10px">
                                    <i class="fa-solid fa-plus"></i> &nbsp; Add
                                </button>
                                </a>
                            </div>
                        </div>
                    </div>
                    <form role="form" action='@Url.Action("GetList", "Module_Payroll_OtherEarning", new { area = "Module" })' id="SaveOtherEarning" method="post">
                       
                        <div class="row">
                            <div class="col-md-6">
                                <div class="row mb-15px">
                                    <label class="form-label col-form-label col-md-4">
                                        Company Name
                                        <span class="important">*</span>
                                    </label>
                                    <div class="col-md-8">
                                        @Html.DropDownListFor(model => model.CmpID, new SelectList(ViewBag.GetCompanyName, "Id", "Name"), "--Select--", new { id = "ddlCmpID", @class = "form-select", name = "CmpId" })
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="row mb-15px">
                                    <label class="form-label col-form-label col-md-4">
                                        Business Unit
                                        <span class="important">*</span>
                                    </label>
                                    <div class="col-md-8">
                                        @Html.DropDownListFor(model => model.BranchId, new SelectList(ViewBag.GetBranchName, "Id", "Name"), "--Select--", new { id = "ddlOtherEarningBranchID", @class = "form-select", name = "BranchId" })
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6">
                                <div class="row mb-15px">
                                    <label class="form-label col-form-label col-md-4">
                                        Earning Name
                                        <span class="important">*</span>
                                    </label>
                                    <div class="col-md-8">
                                        @Html.DropDownListFor(model => model.EarningName, new SelectList(ViewBag.GetHeadVariable, "Id", "Name"), "--Select--", new { id = "ddlVariableHead", @class = "form-select", name = "VariableHead" })
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="row mb-15px">
                                    <label class="form-label col-form-label col-md-4">
                                         Month /Year
                                        <span class="important">*</span>
                                    </label>
                                    <div class="col-md-6">
                                        @Html.TextBoxFor(model => model.OtherEarningMonthYear, new { @class = "form-control", type = "month", id = "dateOtherEarningMonthYear", Name = "OtherEarningMonthYear" })

                                    </div>
                                    <div class="col-md-2">
                                        <button type="button" id="btnshow" onclick="GetList()" class="btn btn-primary btn-sm  mb-10px">
                                            Load
                                        </button>
                                    </div>
                                </div>
                            </div>

                        </div>
                    </form>
                    <!-- BEGIN table-responsive -->
                    <div class="table-scroll div1">
                        <div class="table-wrap">
                            <!-- BEGIN table-responsive -->
                            <table class="table table-bordered align-middle TablePage" id="fixed-header"
                                   style="width: 100%;">
                                <thead>
                                    <tr class="text-center">
                                        @*<th class="text-nowrap width-45">Delete</th>*@

                                        <th class="text-center"><input type="checkbox" class="form-check-input" data-checked="all-checkbox" id="SelectAll" onchange="checkAll(this)" />  All</th>
                                        <th hidden class="text-nowrap">Id </th>
                                        <th class="text-nowrap">Company </th>
                                        <th class="text-nowrap">Business Unit </th>
                                        <th class="text-nowrap">Employee Name</th>
                                        <th class="text-nowrap"> Month /Year  </th>
                                        <th class="text-nowrap">Variable Head</th>
                                        <th class="text-nowrap">Earning Sub-Category</th>
                                        <th class="text-nowrap">Amount</th>
                                        <th class="text-nowrap">Remark</th>

                                    </tr>
                                </thead>
                                <tbody id="tblOtherEarning">
                                    @foreach (var d in ViewBag.OtherEarning)
                                    {
                                        <tr>
                                            <td class="text-nowrap text-center"><input type="checkbox" class="form-check-input" /></td>
                                            @*<td class="text-center">
                                                    <a onclick="Dele teOtherDeduction('@d.VariableDeductionId')" id="btnDelete"
                                                       style="cursor:pointer;">
                                                        <i class="fa-solid fa-trash-alt text-danger fa-lg"></i>
                                                    </a>
                                                </td>*@
                                            <td hidden class="text-nowrap">@d.VariableEarningId</td>
                                            <td class="text-nowrap">@d.CompanyName</td>
                                            <td class="text-nowrap">@d.BranchName</td>
                                            <td class="text-nowrap">@d.employeename</td>
                                            <td class="text-nowrap">@d.MonthYear</td>
                                            <td class="text-nowrap">@d.VariableHead</td>
                                            <td class="text-nowrap">@d.OtherEarningName</td>
                                            <td class="text-nowrap">@d.Amount</td>
                                            <td class="text-nowrap">@d.Remarks</td>

                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                    <div class="row">
                        <div class="LastShortLeaveBx">
                            <div class="row">
                                <div class="col-md-12">
                                    <div class="BtnBx">
                                        <div>
                                            <button type="button" id="btnSubmit" onclick="funDelete()"
                                                    class="btn btn-danger btn-sm">
                                                <i class="fa-solid fa-trash-alt"></i> Delete
                                            </button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- END panel -->
        </div>
    </div>
</div>


<script>
    $(document).ready(function () {
        $("#ddlCmpID").select2();
        $("#ddlOtherEarningBranchID").select2();
        $("#ddlVariableHead").select2();
        var Message = @Html.Raw(Json.Encode(TempData["Message"]));
        var Icon = @Html.Raw(Json.Encode(TempData["Icon"]));
        debugger;
        if (Message != null) {
            if (Icon != null) {
                Swal.fire({
                    icon: Icon,
                    text: Message,
                    allowOutsideClick: () => {
                        const popup = Swal.getPopup()
                        popup.classList.remove('swal2-show')
                        setTimeout(() => {
                            popup.classList.add('animate__animated', 'animate__headShake')
                        })
                        setTimeout(() => {
                            popup.classList.remove('animate__animated', 'animate__headShake')
                        }, 500)
                        // return false
                    }
                }).then(function() {
                    window.location.reload();
                });
            }
        }

        var today = new Date();
        // Format the date as YYYY-MM (year-month)
        var yyyy = today.getFullYear();
        var mm = String(today.getMonth() + 1).padStart(2, '0'); // January is 0!
        var defaultMonth = yyyy + '-' + mm;
        // Set the value of the input element to the default month
        var CheckEmptyDate = document.getElementById('dateOtherEarningMonthYear').value;
        if (CheckEmptyDate == "") {
            document.getElementById('dateOtherEarningMonthYear').value = defaultMonth;
        }
        $("#ddlCmpID").on("change", function () {
            $("#ddlOtherEarningBranchID").find("option").remove();
            $("#ddlVariableHead").find("option").remove();
            var CmpId = $("#ddlCmpID").val();
            $.ajax({
                type: 'GET',
                dataType: 'JSON',
                contentType: "application/json; charset=utf-8",
                url: '@Url.Action("GetBusinessUnit", "Module_Payroll_OtherEarning")',
                data: { CmpId: CmpId },
                success: function (data) {
                    if (data.BranchName !=null) {
                        $("#ddlOtherEarningBranchID").append("<option value=0>--Select-- </option>");
                        $.each(data.BranchName, function(i, p) {
                            $("#ddlOtherEarningBranchID").append("<option value='" + p.Id +"'>" + p.Name +"</option>");
                        });
                    }

                    if (data.GetVeriableHead !=null) {
                        $("#ddlVariableHead").append("<option value=0>--Select-- </option>");
                        $.each(data.GetVeriableHead, function(i, p) {
                            $("#ddlVariableHead").append("<option value='" + p.Id +"'>" + p.Name +"</option>");
                        });
                    }
                    else {
                        $('#ErrorESSPassword').html(" ");
                    }
                }
            })
        })


    });

    //use for add regex function
    jQuery.validator.addMethod(
    "regex",
     function (value, element, regexp) {
         if (regexp.constructor != RegExp)
             regexp = new RegExp(regexp);
         else if (regexp.global)
             regexp.lastIndex = 0;
         return this.optional(element) || regexp.test(value);
     }, "erreur expression reguliere"
  );

    $('#SaveOtherEarning').validate({
        rules: {
            OtherEarningMonthYear: {
                required: true,
            },
            CmpID: {
                required: true,
            },
            BranchId: {
                required: true,
            },
            EarningName: {
                required: true,
            },
        },
        messages: {
            CmpID: {
                required: "Company name is required",
            },
            BranchId: {
                required: "Business unit is required",
            },
            OtherEarningMonthYear: {
                required: "Deduction month/year is required",
            },
            EarningName: {
                required: "Earning name is required",
            },
        },
    });

    var form = $("#SaveOtherEarning");
    form.validate();
    function GetList() {
        debugger;
        form.valid();
        if (form.valid() == false) {
            $("#dateOtherDeductionMonthYear").removeClass("error");
            return false;
        }
        $(".Userloaderbg").css("display", "flex");
        $("#SaveOtherEarning").submit();
    }

    function funDelete() {
        //let table = document.getElementById("tblOtherDeduction");

        //// Get all <tbody> elements in the table
        //let tbodyCount = table.querySelectorAll("tbody").length;
        ////var table = document.getElementById("tblOtherDeduction");

        //// Get the number of rows
        //var rowCount = table.rows.length;
        //alert(rowCount);
        //return false;
        var ObjDelete = [];

        // Iterate through checked checkboxes in the table body only
        $('#tblOtherEarning :checkbox:checked').each(function () {
            var row = $(this).closest('tr'); // Get the closest row
            var data = {
                VariableDeductionId: row.find('td').eq(1).text(), // Hidden column
            };

            // Push the data object to the array
            ObjDelete.push(data);
        });
        if (ObjDelete.length==0)
        {
            var Message="Select atleast one employee";
            var Icon="error";
            if (Message != null) {
                if (Message != null) {
                    Swal.fire({
                        icon: Icon,
                        text: Message,
                        allowOutsideClick: () => {
                            const popup = Swal.getPopup()
                            popup.classList.remove('swal2-show')
                            setTimeout(() => {
                                popup.classList.add('animate__animated', 'animate__headShake')
                            })
                            setTimeout(() => {
                                popup.classList.remove('animate__animated', 'animate__headShake')
                            }, 500)

                            return false
                        }
                    })
                    return false;
                }
            }
        }
        const DeleteButtons = Swal.mixin({
            customClass: {
                cancelButton: ' btnpress',
                confirmButton: ' btnpress'
            },
            buttonsStyling: true
        })
        DeleteButtons.fire({
            title: 'Are you sure?',
            text: "You won't be able to revert this!",
            icon: 'warning',
            confirmButtonColor: '#d33',
            cancelButtonColor: '#3085d6',
            showCancelButton: true,
            confirmButtonText: 'Yes, delete it!',
            cancelButtonText: 'No, cancel!',
            reverseButtons: false,

            allowOutsideClick: () => {
                const popup = Swal.getPopup()
                popup.classList.remove('swal2-show')
                setTimeout(() => {
                    popup.classList.add('animate__animated', 'animate__headShake')
                })
                setTimeout(() => {
                    popup.classList.remove('animate__animated', 'animate__headShake')
                }, 500)
                return false
            }

        }).then((result) => {
            if (result.isConfirmed) {
                debugger;
                var ObjDelete = [];

                // Iterate through checked checkboxes in the table body only
                $('#tblOtherEarning :checkbox:checked').each(function () {
                    var row = $(this).closest('tr'); // Get the closest row
                    var data = {
                        VariableDeductionId: row.find('td').eq(1).text(), // Hidden column
                    };

                    // Push the data object to the array
                    ObjDelete.push(data);
                });
                if (ObjDelete.length==0)
                {
                    var Message="Select atleast one employee";
                    var Icon="error";
                    if (Message != null) {
                        if (Message != null) {
                            Swal.fire({
                                icon: Icon,
                                text: Message,
                                allowOutsideClick: () => {
                                    const popup = Swal.getPopup()
                                    popup.classList.remove('swal2-show')
                                    setTimeout(() => {
                                        popup.classList.add('animate__animated', 'animate__headShake')
                                    })
                                    setTimeout(() => {
                                        popup.classList.remove('animate__animated', 'animate__headShake')
                                    }, 500)

                                    return false
                                }
                            })
                            return false;
                        }
                    }
                }
                //return false;
                $(".Userloaderbg").css("display", "flex");
                $.ajax({
                    type: "POST",
                    url: '@Url.Action("Delete", "Module_Payroll_OtherEarning")',
                    data: JSON.stringify({ ObjDelete: ObjDelete }),
                    contentType: "application/json; charset=utf-8",
                    dataType: "text",
                    success: function (response) {
                        debugger;
                        var data = JSON.parse(response);
                        var Message1 = data.Message;
                        var Icon1 = data.Icon;
                        if (data != null) {
                            if (Message1 != null) {
                                Swal.fire({
                                    icon: Icon1,
                                    text: Message1,
                                    allowOutsideClick: () => {
                                        const popup = Swal.getPopup()
                                        popup.classList.remove('swal2-show')
                                        setTimeout(() => {
                                            popup.classList.add('animate__animated', 'animate__headShake')
                                        })
                                        setTimeout(() => {
                                            popup.classList.remove('animate__animated', 'animate__headShake')
                                        }, 500)
                                        return false
                                    }
                                }).then((result) => {
                                    if (result.isConfirmed) {
                                        window.location.href = '@Url.Action("GetList", "Module_Payroll_OtherEarning")';
                                    } else if (
                                        /* Read more about handling dismissals below */
                                        result.dismiss === Swal.DismissReason.cancel
                                    ) {

                                    }
                                })
                            }
                            // location.reload();
                        }
                    }
                });
                @*window.location.href = '@Url.Action("Delete", "Module_Payroll_OtherDeduction")?VariableDeductionId=' + VariableDeductionId;*@

            } else if (
                /* Read more about handling dismissals below */
                result.dismiss === Swal.DismissReason.cancel
            ) {

            }
        })
    }

    function checkAll(ele) {
        var checkboxes = document.getElementsByTagName('input');
        if (ele.checked) {
            for (var i = 0; i < checkboxes.length; i++) {
                if (checkboxes[i].type == 'checkbox') {
                    checkboxes[i].checked = true;
                }
            }
        } else {
            for (var i = 0; i < checkboxes.length; i++) {
                console.log(i)
                if (checkboxes[i].type == 'checkbox') {
                    checkboxes[i].checked = false;
                }
            }
        }
    }

</script>
