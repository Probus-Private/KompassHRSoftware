
@{
    ViewBag.Title = "ViewListList";
    Layout = "~/Views/Shared/_Layout.cshtml";
    //var action = ViewBag.module == "Lead" ? "Lead_Dashboard" : "OpportunityList";
}

@{
    ViewBag.Title = "MAS_ReportNameList";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var action = ViewBag.module == "Lead" ? "Lead_Dashboard" : "OpportunityList";
}

<div id="content" class="app-content">
 <!-- END page-header -->
    @*<div class="d-flex flex-wrap">*@
    <div class="row">
        <ol style="list-style: none; padding: 0; margin: 0; ">
            <li style="float:right; margin-right: 15px;">
                <a href="@Url.Action("Lead_Dashboard", "CRM",  new {area = "CRMS" })">
                    <img src="~/assets/MenuImages/Back.png" style="max-width: 100%; height: auto;" alt="Back">
                </a>
            </li>
        </ol>

        @*<button type="button" class="btn btn-danger me-1 mb-1" onclick="window.location.href='@Url.Action(action, "CRM",new { area ="CRM"})'">
                <i class="fa fa-arrow-left fa-sm" title="Back" style="font-size: x-small;"></i> Back
            </button>*@
    </div>

    <div class="panel panel-inverse" id="ShiftRuleId">
        <!-- BEGIN panel-heading -->
        <div class="panel-heading">
            <h4 class="panel-title">
                Report Name List
            </h4>
            <div class="panel-heading-btn">
                <a href="@Url.Action("Lead_Dashboard", "CRM", new { area = "CRMS" })" class="btn btn-xs btn-icon btn-default">
                    <i class="fa fa-home fa-lg"></i>
                </a>
                <a href="javascript:;" class="btn btn-xs btn-icon btn-default" data-toggle="panel-expand"><i class="fa fa-expand"></i></a>
                <a href="javascript:;" class="btn btn-xs btn-icon btn-success" data-toggle="panel-reload"><i class="fa fa-redo"></i></a>
                <a href="javascript:;" class="btn btn-xs btn-icon btn-warning" data-toggle="panel-collapse"><i class="fa fa-minus"></i></a>
                <a href="javascript:;" class="btn btn-xs btn-icon btn-danger" data-toggle="panel-remove"><i class="fa fa-times"></i></a>
            </div>
        </div>

        <!-- END panel-heading -->
        <!-- BEGIN panel-body -->
        <div class="panel-body" id="LEAType">
            <div class="row">
                <div class="col-12 overflow-auto">

                    <table id="data-table-combine15" class="table table-striped table-bordered align-middle">
                        <thead>
                            <tr>
                                @*<th class="width-45"></th>*@
                                <th class="text-nowrap text-center">Action</th>
                                <th class="text-nowrap">Report Name</th>
                                <th class="text-nowrap">Description</th>
                            </tr>
                        </thead>

                        <tbody>

                            @foreach (var item in ViewBag.ReportNameList)
                            {
                                <tr>
                                    <td class="width-45 text-center">
                                        <a href="#" onclick="location.href='@Url.Action($"{@item.ViewName}", "CRM",new {area ="CRMS"})'" title="GO To"> <i class="far fa-lg fa-fw fa-circle-dot" aria-expanded="false"></i></a>
                                    </td>
                                    <td>@item.ReportName</td>
                                    <td>@item.Description</td>

                                </tr>

                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

</div>







